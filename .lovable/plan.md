

## Update Version Number to v0.8.0-alpha

Update the version string in two places to reflect the cumulative work done across our conversation:

### Changes

1. **`src/pages/Settings.tsx` (line 38)**: Change `APP_VERSION` from `'1.0.0'` to `'0.8.0-alpha'`

2. **`SECURITY.md` (line 25)**: Update the supported versions table from `v0.7-alpha` to `v0.8.0-alpha`

